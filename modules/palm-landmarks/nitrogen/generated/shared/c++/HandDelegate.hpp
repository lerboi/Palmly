///
/// HandDelegate.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::palmly {

  /**
   * An enum which can be represented as a JavaScript union (HandDelegate).
   */
  enum class HandDelegate {
    GPU      SWIFT_NAME(gpu) = 0,
    CPU      SWIFT_NAME(cpu) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::palmly

namespace margelo::nitro {

  // C++ HandDelegate <> JS HandDelegate (union)
  template <>
  struct JSIConverter<margelo::nitro::palmly::HandDelegate> final {
    static inline margelo::nitro::palmly::HandDelegate fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("gpu"): return margelo::nitro::palmly::HandDelegate::GPU;
        case hashString("cpu"): return margelo::nitro::palmly::HandDelegate::CPU;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum HandDelegate - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::palmly::HandDelegate arg) {
      switch (arg) {
        case margelo::nitro::palmly::HandDelegate::GPU: return JSIConverter<std::string>::toJSI(runtime, "gpu");
        case margelo::nitro::palmly::HandDelegate::CPU: return JSIConverter<std::string>::toJSI(runtime, "cpu");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert HandDelegate to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("gpu"):
        case hashString("cpu"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
