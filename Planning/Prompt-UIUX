/loop You are executing the Palmly UI/UX redesign v2 ("Vermilion & Motion"). Work autonomously and continue until every v2 task is done or you are blocked.

GROUNDING (do this at the start of every iteration, before any work):
1. Read Planning/UIUX-Redesign-v2.md IN FULL — it is the design source of truth (the Vermilion color system, the three-reds discipline, the motion foundation, the design-system primitives, per-surface identity principles, and the device-free verification method). Locked direction (2026-07-15): a MODERN VERMILION accent (light #D8402C / dark #FF7C63) as the single accent — indigo is FULLY RETIRED (red is the only accent, over the calm neutral base); it is a modern red, NOT the old dated cinnabar. Heritage red is deepened to a claret (#9E3B2E / #E0806F) reserved ONLY for the red-thread motif + corner seal; danger crimson is destructive-only. Motion is TASTEFUL & PREMIUM (foundation-first) and EVERY animation has a reduce-motion fallback. This doc overrides only the color/motion of the older UIUX-Redesign.md — that doc's type, elevation, in-house icon set, English-first, a11y, and server-reskin method still apply.
2. Read Planning/UIUX-Redesign-v2-Tasks.md IN FULL — the checkbox task ledger (STATE block + Execution Protocol + tasks V1..V23). This is the machine you execute.
3. For the screen/flow you are about to touch, read its behavior/content spec in Planning/UIUX-specs.md, and READ the current source file(s) before editing (match surrounding style).

EXECUTE:
4. Follow the ledger's EXECUTION PROTOCOL literally: pick the first task in document order whose checkbox is [ ] or [~]; build it to the north star using ONLY design tokens (no raw hexes in components — the camera OVERLAY neutrals are the one justified exception, and even its identity colors now read from accent) and realistic English content (no lorem, no CJK); honor the THREE-REDS DISCIPLINE (accent=vermilion everywhere incl. the palm-line highlight; heritageAccent=claret for the red-thread + seal ONLY; danger=crimson for destructive ONLY); run its Verify line; and ONLY when verification actually passes, mark it [x] with today's date, append a Build Log line with evidence, update the STATE block, and git commit as "V# <description>". Then proceed to the next task.
5. NEVER reorder tasks — the V0 FOUNDATION (V1 accent skin, V2 motion tokens + the shared useReducedMotion hook, V3 Button, V4 Card, V5 Logomark, V6 PalmDiagram, V7 AppHeader, V8 /dev/theme) MUST land before any screen task (V9+), because every screen consumes the new tokens, the motion hook, and the upgraded primitives. Every animation you add routes through useReducedMotion() and the gate `shouldAnimate = !reduceMotion && Platform.OS !== 'web'`; every new svg icon/animation gets an accessibilityLabel / reduce-motion fallback.
6. Verify device-free, like the existing R1–R24 checkpoints: static gates first (from app/: `npm run typecheck`, `npm run lint`, `npm test`; for edge-function tasks also the Deno tests), then a visual check via `npx expo export --platform web` + a headless-Chrome screenshot at 390x844 (and 320 wide where clipping matters), saved under docs/checkpoints/redesign/. The /dev route map walks every route; /dev/theme is the design-system regression surface. Reanimated motion, haptics, the camera feed, the native OS share sheet, and RevenueCat are native-only: they render as their STATIC END-STATE on web (that is the reduce-motion/web gate working) — screenshot the finished frame, verify the layout, and mark the live leg [~] (built, pending device). NEVER report a green you did not actually see.
7. NEVER mark a task [x] without its Verify passing. If a task fails 3 genuinely different approaches, mark it [!], record what you tried under it, set STATE "Blocked on", and move to the next unblocked task.
8. Standing rules: do NOT break backend/edge-function logic, auth, realtime, or the device-gated stubs — this is a visual/UX overhaul, not new features (the only server edits are re-skinning the SVG/HTML in V21). Preserve ThemeProvider's forceScheme prop (the /dev/theme harness needs it). Ship back-compat token aliases in V1 (gold→premium, jade→success, seal→heritageAccent, background→bg, text→textPrimary) rather than rename-and-break; re-pin tokens.test.ts and geometry.test.ts to the new contract rather than deleting them. Migrations/secrets: unchanged — none of this touches the DB schema. Decisions the docs don't cover: pick the simplest option that matches the north star and note it in the ledger.

LOOP CONTROL:
9. Keep completing tasks while context allows. Before context runs low, finish or cleanly park the in-flight task as [~] with a resume note, update STATE, commit everything, and end the iteration — the next iteration re-grounds from step 1.
10. If ALL remaining work is blocked ([!] or genuinely needs me), give me a clear list of exactly what you need, then STOP the loop.
11. When every task is [x] (or the rest are [!]), report it and stop the loop.
12. End every iteration with a short status report: tasks completed this run (with screenshot/test evidence), current STATE, and anything you need from me.
