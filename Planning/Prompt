/loop You are executing the Palmly MVP build. Work autonomously and continue until you are blocked or the MVP is shipped.

GROUNDING (do this at the start of every iteration, before any work):
1. Read C:\Users\leheh\.Projects\Palmly\Planning\MVP_Buildplan.md in full. It is the single source of truth for build progress: a sequential task ledger with a STATE block, an Execution
Protocol, phases P0–P12, and per-task Verify procedures.
2. The three spec documents are the authoritative detail behind every task — consult the exact sections each task references before building it:
    - C:\Users\leheh\.Projects\Palmly\Planning\mvp_spec.md (product vision and scope — ground truth)
    - C:\Users\leheh\.Projects\Palmly\Planning\Backend-specs.md (architecture, schema, RLS, AI pipeline on Gemini, queues, deep links, retention/deletion policy)
    - C:\Users\leheh\.Projects\Palmly\Planning\UIUX-specs.md (visual identity, every screen, capture state machine, share card spec, notifications)
    Read the sections relevant to the current task — do not build from memory of them.

EXECUTE:
3. Follow the build plan's EXECUTION PROTOCOL literally. In short: find the first task in document order whose checkbox is [ ] or [~]; execute its Build instruction; run its Verify
procedure; only when verification actually passes, mark it [x] with today's date, append a Build Log line with evidence, update the STATE block, and git commit as "P#.T# <description>".
Then proceed to the next task.
4. NEVER mark a task done without its verification passing. NEVER skip ahead or reorder, except tasks marked ∥ (parallel-safe) when a HUMAN task blocks the sequence.
5. 🧑 HUMAN tasks: do not attempt them. Check for the evidence their Verify line specifies (keys in .env, dashboards, repo secrets). If present, mark done and continue. If absent, write
exactly what I need to do into the STATE block's "Notes for next run", then work any available ∥ tasks.
6. If a task fails verification after 3 genuinely different approaches, mark it [!], record what you tried under the task, set STATE "Blocked on", and treat it as a blocker.
7. Decisions the specs don't cover: choose the simplest option that passes verification and record it in the build plan's DECISION LOG. Never silently deviate from the specs.

LOOP CONTROL:
8. Keep completing tasks while context allows. Before context runs low, finish or cleanly park the in-flight task as [~] with a resume note, update STATE, commit everything, and end the
iteration — the next iteration re-grounds from step 1.
9. If ALL remaining work is blocked on me (human tasks or [!] blockers), give me a clear, complete list of exactly what I must do, then STOP the loop entirely — do not keep idling.
10. If the STATE block says MVP SHIPPED, report it and stop the loop.
11. End every iteration with a short status report: tasks completed this run (with evidence), current STATE, and anything you need from me.

One quick note on the .env file you have open: my .gitignore ignores .env and all .env.* (except .env.example), so it's safe from commits — but the build plan expects .env.staging /
.env.prod for backend keys and app/.env for the client EXPO_PUBLIC_* keys. See docs/ENVIRONMENT.md §6 for exactly which key goes in which file. A single root .env will work for local
scripts but won't be picked up by the Expo app bundle (which only reads app/.env).