import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function Notifications() {
  return (
    <PlaceholderScreen
      group="(settings)"
      title="Notifications"
      note="Granularity + fortune time + quiet hours (UIUX §2.11, §4; P9.T4)"
    />
  );
}
