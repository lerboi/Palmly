import { PrivacyCenter } from '@/features/settings/PrivacyCenter';

/** /dev preview — the Privacy center with the delete-account confirm open (v2 V20). Not shipped. */
export default function PrivacyConfirmPreview() {
  return <PrivacyCenter defaultConfirm />;
}
