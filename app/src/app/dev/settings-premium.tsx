import { SettingsHub } from '@/features/settings/SettingsHub';

/** /dev preview — the Settings hub in the premium plan state (v2 V20). Not shipped. */
export default function SettingsPremiumPreview() {
  return <SettingsHub premium />;
}
