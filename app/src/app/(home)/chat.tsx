import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function Chat() {
  return (
    <PlaceholderScreen
      group="(home)"
      title="Ask the reader"
      note="Premium chat, grounded on your reading features + KB (UIUX §2.11, P9.T6)"
    />
  );
}
